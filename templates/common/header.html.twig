<nav class="navbar navbar-expand-lg bg-dark bg-gradient fs-5" data-bs-theme="dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path ('app_main') }}"><img height="80px" width="150px" src="{{ asset('assets/img/logo.webp') }}" alt="Logo de Sortir.com"></a>
        <h1 class="header_h1">Sortie.com</h1>
        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav ms-auto">

                {% if is_granted("ROLE_ADMIN") %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path ('app_city_index') }}">Ville</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_site_index') }}">Sites</a>
                    </li>
                {% endif %}



                {% if app.user %}

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_activity_index') }}">Accueil</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_participant_edit', {'id' : app.user.participant.id}) }}">Mon profil</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}">Se déconnecter</a>
                    </li>

                {% else %}

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login') }}">Se connecter</a>
                    </li>

                {% endif %}

            </ul>
        </div>
    </div>
</nav>