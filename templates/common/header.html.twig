<nav class="navbar navbar-expand-lg bg-primary fs-5" data-bs-theme="dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="#"><img height="125px" width="125px" src="{{ asset('assets/img/logo.png') }}" alt="Logo de Sortir.com"></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav ms-auto">

                {% if is_granted("ROLE_ADMIN") %}
                    <li class="nav-item">
                        <a class="nav-link" href="#">Ville</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="#">Sites</a>
                    </li>

                {% endif %}

                <li class="nav-item">
                    <a class="nav-link" href="#">Accueil</a>
                </li>

                {% if app.user %}

                    <li class="nav-item">
                        <a class="nav-link" href="#">Mon profil</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="#">Se déconnecter</a>
                    </li>
                {% else %}

                    <li class="nav-item">
                        <a class="nav-link" href="#">Se connecter</a>
                    </li>

                {% endif %}

            </ul>
        </div>
    </div>
</nav>